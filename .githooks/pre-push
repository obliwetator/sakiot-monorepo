#!/bin/sh
set -eu

repo_root=$(git rev-parse --show-toplevel)
cd "$repo_root"

echo "Checking Rust formatting..."
cargo fmt --all --check

echo "Checking frontend formatting..."
(
	cd sakiot_stage
	bunx biome check .
)
